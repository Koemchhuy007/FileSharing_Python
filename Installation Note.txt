To up the server, you need to install
-install latest version of python programming
-install pip of python 
-pip install flask python( pip install flask or pip install -r requirements.txt)
-up the server (python server.py)
